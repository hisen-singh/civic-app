## TASK-009-FIX-01 approval & commit plan

Read-only review is complete — verdict: APPROVED pending independent test verification.

### Step 1 — Verify tests independently
Run `npx jest --silent` and confirm 9/9 suites, 55/55 tests green (worker claimed the same; manager verifies, never trusts).

### Step 2 — Record review verdicts (manager-owned files only)
- Append MANAGER REVIEW section to `.agents/results/TASK-009-FIX-01.md`: verdict APPROVED; diff matches spec exactly (App.js +1 line, navigate target, back button with `insets.top + 8` positioning, shown for all `UserProfile` presentations, never on tab).
- Append nit note: back button lives inside the ScrollView so it scrolls with content — optional follow-up, not blocking (hardware back works; no acceptance criterion violated).
- Update `.agents/state/PROJECT-STATE.md`: TASK-009 + FIX-01 = COMPLETED (PASS); mark known-gap items #1 (no profile entry points) / #2 (dead-end FollowList tap) as resolved by this chain.

### Step 3 — Commit the TASK-009 chain
Single commit with the three code files + `.agents` bookkeeping (tasks, results, state):
`feat(profile): other-user profiles via UserProfile stack route (TASK-009 + FIX-01)`
The husky pre-commit hook (eslint --fix + prettier) will normalize the worker's formatting — if the hook fails, fix formatting and retry; never `--no-verify`.

### Step 4 — Prepare next delegation task (no implementation)
Write `.agents/tasks/TASK-010.md` for the next roadmap item: audit #13 — sanitize YouTube URL query params in `utils/timeAgo.js` (strip everything except the `v` param on youtube.com, `youtu.be/<id>` path form only), with tests. Small, security-adjacent, zero architectural risk. Worker picks it up when free.

### Explicitly NOT in this plan
- No changes to services, contexts, rules, or any code beyond committing already-reviewed worker output.
- No Firebase deploy (human action), no API-key rotation (human action).