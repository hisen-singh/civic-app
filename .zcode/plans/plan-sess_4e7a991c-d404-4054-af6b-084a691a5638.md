## Goal
Restyle the Home screen like the reference app (Bilibili-style feed) in CIVIC's dark theme, keep all existing CIVIC buttons/actions — and swap the orange accent app-wide to **Teal #14B8A6** (activism psychology: "go" / collective efficacy / constructive progress).

**Confirmed decisions:** stories row stays (with pts) · "Report an Issue" button removed from feed · full-width cards like the image · accent = teal.

## 1. `screens/HomeScreen.js` — top bar + feed header
- Fixed top header: pill search bar (magnifier + `t("home.search_placeholder")`, tap → existing Search screen) with **CIVIC logo + name on the right side**, then bell icon. "Sign In" chip leaves the top bar (signed-out banner in feed still handles sign-in).
- Feed header order: sign-in banner (signed out) → stories row (kept) → underline category tabs → feed. Search bar + Report button removed from feed body.
- All data logic (pagination, refresh, empty/error states, LoginOverlay) untouched.

## 2. `components/ui/FilterPills.js` — underline variant
Add `variant` prop; default `"pill"` unchanged for other screens. Home uses `variant="underline"`: text-only tabs — active teal text + short teal underline bar, inactive muted, no icons/borders.

## 3. `components/IssueCard.js` — full-width feed card
- No margins/border/radius: 16:9 media edge-to-edge, small dark gap between posts. Title/description move **below** the media on card background.
- Urgency chip + status badge stay overlaid on media; YouTube play button stays; 3px urgency edge bar dropped.
- Author row (avatar, name, time, location) + engagement row below: upvote / comment / share / flag as clean icon+count buttons (no pill borders); "Help Solve" / "Mark Fixed" remain teal pill buttons.
- All behavior unchanged.

## 4. `components/ui/SkeletonCard.js`
Adjusted to new full-width shape (media block + text lines).

## 5. Accent color swap — orange → teal (app-wide)
New tokens in `theme.js` (the `theme` object):
- `accentBrand: #14B8A6`, `accentBrandLight: #2DD4BF`, `accentBrandSubtle: rgba(20,184,166,0.12)`, `accentGlow: rgba(20,184,166,0.25)`, `actionHover: #14B8A6`
- `gradients.cta: ["#2DD4BF", "#14B8A6"]`, `glowButton: ["#2DD4BF", "#0D9488"]`, glow/accentGlow `shadowColor: #14B8A6`

Replace hardcoded values (same alphas) in: `components/NativeImpactCard.js`, `components/ui/CategoryGrid.js`, `components/BottomSheet.js`, `components/ReportBottomSheet.js`, `components/ui/MediaPicker.js`, `screens/MapScreen.js`, `screens/WatchAreaScreen.js`, `screens/ReportIssueScreen.js` (+ any final-grep stragglers):
- `#FF4500 → #14B8A6` · `#FF6A00/#FF7A45 → #2DD4BF` · `rgba(255,69,0,x) → rgba(20,184,166,x)`
- Status colors (critical red etc.) unchanged. Note: `assets/logo.jpg` image keeps its original colors.

## Not changing
Bottom tab bar structure (center + FAB already matches reference), other screens' logic, services, Firebase.

## Verification
Babel syntax-parse every edited file; final grep proves no `#FF4500` / `rgba(255,69,0` remain (outside node_modules). Visual check on your emulator reload.

**Side note (out of scope):** `services/IssueService.js` has two conflicting `searchIssues` definitions (lines 282, 390) — the second silently wins; worth deduplicating later.